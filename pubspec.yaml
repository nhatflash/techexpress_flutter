name: techexpress_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  dio: ^5.9.1
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.0.0
  jwt_decoder: ^2.0.1
  logger: ^2.6.2
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
